# Shared styles

`site.css` is the single shared stylesheet for all Italian and English pages. It defines design tokens, typography, navigation, responsive behavior, galleries, cards, catalog layouts, and lightbox presentation.

A selector change may affect every public page. Preserve accessible contrast, mobile navigation breakpoints, image aspect behavior, and both static and JavaScript-rendered catalog layouts. Preview representative pages at mobile and desktop widths after visual changes.
