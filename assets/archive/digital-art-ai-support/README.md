# Digital art with AI support archive (`DA`)

Contains 16 PNG artwork images (`DA-0001` to `DA-0016`) for the “Digital art with AI support” collection.

Metadata and public paths are controlled by `../../data/archive.js` and `../../data/archive.json`. Keep factual distinctions between original artworks, AI-supported transformations, and technical processing; do not add unsupported process claims.
