# Archive grid previews

This folder contains 326 lightweight WebP derivatives for the paintings-photo archive grid. Files are generated, not hand-edited, and correspond one-to-one with the technical IDs in `assets/data/paintings-archive.json`.

Regenerate them together with the `full/` variants by running `10_Working/build_paintings_archive.py` against the external, read-only source folder.
