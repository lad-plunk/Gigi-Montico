# Working tools

This folder contains reproducible project-maintenance scripts, not public website content.

- `build_paintings_archive.py` reads an external, read-only artwork-photo folder and generates the two WebP resolutions plus the synchronized website manifests.

Run scripts from any working directory. They derive the repository root from their own location; external source folders must be passed explicitly.
