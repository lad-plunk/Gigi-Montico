# Agent configuration

This reserved folder is for repository-specific agent configuration if such files are added later. It currently contains no runtime website content.

Keep website source and assets outside this folder. General project instructions belong in the root `AGENTS.md`; document any future tool-specific file here before relying on it.
